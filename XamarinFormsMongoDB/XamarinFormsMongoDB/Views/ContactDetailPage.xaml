<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsMongoDB.Views.ContactDetailPage">
  <StackLayout Orientation="Vertical">
    <Entry x:Name="nameEntry" Text="{Binding Name}"/>
    <Entry x:Name="lastNameEntry" Text="{Binding LastName}"/>
    <StackLayout Orientation="Horizontal">
      <Button x:Name="saveButton" Command="{Binding SaveCommand}"/>
      <Button x:Name="deleteButton" Command="{Binding DeleteCommand}"/>
    </StackLayout>
  </StackLayout>
</ContentPage>